<UserControl
    x:Name="Root"
    x:Class="UniSky.Controls.Sheet.SheetRootControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniSky.Controls.Sheet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <!-- one SheetPresenter per open sheet, stacked back to front. collapsed while the stack is
         empty so this layer never eats input meant for the app behind it. -->
    <Grid x:Name="SheetHost"
          Visibility="Collapsed"/>
</UserControl>
